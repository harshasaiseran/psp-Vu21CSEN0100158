#include <stdio.h>
int main()
{
    int num,rem,sum=0,i;
    printf("enter the number\n");
    scanf("%d",&sum);
    for(i=1;i<num;i++)
    {
        rem=num%i;
        if(rem==0)
     {
        sum=sum+i;
     }
    }
    if(sum=sum){
        printf("%d is a perfect num");
    }
    else
    printf("%d is not a perfect num");
    return 0;
}

